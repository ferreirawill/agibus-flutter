import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:agibus_mobile/app/modules/home/pages/stations/stations_page.dart';

void main() {
  testWidgets('StationsPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(StationsPage(title: 'Stations')));
    //  final titleFinder = find.text('Stations');
    //  expect(titleFinder, findsOneWidget);
  });
}
